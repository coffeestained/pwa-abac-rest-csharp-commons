namespace Common.Global;

public static class Utils 
{

    // Accepts a value and compares it
    // to a List of comparators. 
    // One of these comparators must return true
    static public bool Validator(object value, object[] validators) 
    {
        var isValid = true;
        return isValid;
    }

}
